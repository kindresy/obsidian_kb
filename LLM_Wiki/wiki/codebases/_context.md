This directory contains codebase overview pages (one per repository).
Each page is generated by `kb code compile <repo-name>` and provides
a high-level summary of the repo's architecture, layers, and entry points.
