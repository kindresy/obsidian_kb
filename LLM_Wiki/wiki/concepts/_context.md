This directory contains concept, person, and source-summary pages for all knowledge domains (PCIe, IOMMU, I3C, etc.). Each page uses `[[wikilinks]]` to cross-reference related concepts.

Pages here should NOT be linked directly from the index -- they are organized under their respective domain headings in `index.md`.
